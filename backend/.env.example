# ───── Core Settings ─────
API_V1_STR=/api/v1
VERSION=0.1.0
PROJECT_NAME=AI Codebase Backend
ENVIRONMENT=local

# ───── Secrets & Tokens ─────
# Your own symmetric key for JWT signing
SECRET_KEY=replace-with-your-own-random-secret

# JWT expiry
ACCESS_TOKEN_EXPIRE_MINUTES=60

# ───── CORS ─────
# Comma-separated list or JSON array
FRONTEND_CORS_ORIGINS=http://localhost:3000

# ───── Service URLs ─────
# Where your API will be hosted
SERVER_HOST=http://localhost:8000

# ───── GitHub OAuth ─────
GITHUB_CLIENT_ID=your-github-app-client-id
GITHUB_CLIENT_SECRET=your-github-app-client-secret
GITHUB_REDIRECT_URI=http://localhost:8000/api/v1/auth/callback

# ───── AI / Vector DB ─────
# OpenAI API key for embeddings & LLM calls
OPENAI_API_KEY=sk-...
# Your Chroma (or other vector store) base URL
CHROMA_DB_URL=http://localhost:8000
